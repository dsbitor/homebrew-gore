# homebrew-gore

Homebrew tap for [gore](https://github.com/dsbitor/gore-releases),
a batch orchestration CLI. Formula downloads and verifies the same
checksummed release archives gore-releases already publishes; this
tap builds nothing itself.

```bash
brew install dsbitor/gore/gore
```
